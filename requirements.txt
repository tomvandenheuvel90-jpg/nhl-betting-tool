# Bet Analyzer — dependencies
# Streamlit Cloud: dit bestand wordt automatisch gebruikt bij deploy

# Web framework (Streamlit Cloud)
streamlit>=1.35

# Claude API
anthropic>=0.25

# NBA stats (gratis, geen key)
nba_api>=1.4

# Data
pandas>=2.0
requests>=2.31

# Flask (alleen lokaal, niet nodig op Streamlit Cloud)
flask>=2.3
