# Data directories (grote bestanden, niet in repo)
moneypuck_data/raw/
moneypuck_data/filtered/raw/
nba_data/
mlb_data/
football_data/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Environment & secrets
.env
api_keys.txt
.stats_cache.json
gdrive_credentials.json
*credentials*.json
*service-account*.json

# Claude Code interne bestanden / worktrees (niet in repo)
.claude/

# LibreOffice / Office lock files
.~lock.*#
~$*.docx
~$*.xlsx

# Lokale runtime data (per-machine, niet delen)
analyse_geschiedenis.json
odds_api_usage.json
settings.json
bankroll_mutations.json

# macOS
.DS_Store

# Streamlit — secrets niet in repo, maar .example wel
.streamlit/secrets.toml
.streamlit/*.toml
!.streamlit/secrets.toml.example

# Cache bestanden
.bet_cache.json

# Tijdelijke bestanden
*.tmp
*.log
~$*
